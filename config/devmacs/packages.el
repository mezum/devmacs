;;; packages.el --- packages for the user configuration -*- lexical-binding: t; -*-

;; Install these with `ee --user-install', or M-x devmacs-user-install from
;; inside Emacs. They are written to the state volume, so they survive image
;; updates and only have to be installed once.

;;; Code:

(setq devmacs-user-packages
      '(;; rainbow-delimiters
        ;; puni
        ))

;;; packages.el ends here
